import React from 'react';

const Filter = () => {
  return (
    <div>
      <select>
        <option>All</option>
        <option>Complete</option>
        <option>In Progress</option>
      </select>
    </div>
  );
};

export default Filter;
